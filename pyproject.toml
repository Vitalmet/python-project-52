[project]
name = "python-project-52"
version = "0.1.0"
description = "Task Manager Django Application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []